#include <stdio.h>

int main() {

    printf("We could test our 'wc' program using a BASH script or we could make the whole program a function.\n");
    printf("Then we could invoke the function with various strings and assert the output counts\n");
    printf("Some inputs I would use would be the empty string, a bunch of spaces, a bunch of tabs.\n");
    printf("A bunch of newlines, strings with no spaces, tabs, or newlines... edge cases.\n");
}
